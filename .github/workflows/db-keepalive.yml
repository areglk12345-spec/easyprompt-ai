name: DB keep-alive ping

on:
  schedule:
    - cron: '0 3 */2 * *'  # every 2 days at 03:00 UTC
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit backend DB health check
        run: curl -fsS https://api.verbaqo.com/health/db
